generator client {
  provider      = "prisma-client"
  output        = "../generated"
  moduleFormat  = "esm"
  runtime       = "bun"
  binaryTargets = ["native", "linux-musl"]
}

datasource db {
  provider = "postgresql"
}
